export interface BaseRepositoryInterface<Entity> {
  // Create
  // createMany(): Promise<boolean>;
  // // Read
  // findOne<T = Entity>(id: number): Promise<T>;
  // findOneById<T = Entity>(id: string, options?: unknown): Promise<T>;
  // findAll<T = Entity>(): Promise<T[]>;
  // // Update
  // update(): Promise<Entity>;
  // updateMany(): Promise<boolean>;
  // // Delete
  // delete(): Promise<Entity>;
  // softDelete(): Promise<Entity>;
  // deleteMany(): Promise<boolean>;
  // deleteManyByIds(ids: string[]): Promise<boolean>;
  // Utils
  // getTotal(): Promise<number>;
  // exists(): Promise<boolean>;
}
